import { createStore } from 'vuex'
// vue 는 공식적으로 'vuex' 라는 서비스를 import 채택

export default createStore({
  // 앱의 상태를 관리할 수 있음
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
})
